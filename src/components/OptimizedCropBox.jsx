/**
 * SIMPLE CROP BOX COMPONENT - PRESERVER backup copy 
 * RUNTIME DISABLED due to previous data corruption issues
 * Please use smoothResizeCropBox.main instead for all applications
 */
export const OptimizedCropBox = null;